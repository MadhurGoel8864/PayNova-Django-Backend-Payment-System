[variables]
NIXPACKS_PYTHON_VERSION = "3.10"

[phases.setup]
nixPkgs = [
  "python310",
  "pip",
  "gcc",
  "postgresql_16"
]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python manage.py migrate && gunicorn payment_prj.wsgi:application"
